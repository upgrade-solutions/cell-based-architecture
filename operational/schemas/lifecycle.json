{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://dna.local/operational/lifecycle",
  "title": "Lifecycle",
  "description": "A Lifecycle describes the valid sequence of Capabilities across the life of a Noun — which states it can move through and in what order.",
  "type": "object",
  "required": ["noun", "steps"],
  "properties": {
    "noun": {
      "type": "string",
      "description": "The Noun whose lifecycle this describes.",
      "examples": ["Loan", "Order", "Claim", "Employee"]
    },
    "description": {
      "type": "string",
      "description": "A human-readable explanation of this lifecycle."
    },
    "steps": {
      "type": "array",
      "description": "The primary sequence of capabilities in this lifecycle, in order.",
      "minItems": 1,
      "items": {
        "type": "string",
        "pattern": "^[A-Z][a-zA-Z]*\\.[A-Z][a-zA-Z]*$"
      }
    },
    "branches": {
      "type": "array",
      "description": "Alternative paths that diverge from the primary sequence.",
      "items": {
        "type": "object",
        "required": ["from", "to"],
        "properties": {
          "from": {
            "type": "string",
            "pattern": "^[A-Z][a-zA-Z]*\\.[A-Z][a-zA-Z]*$"
          },
          "to": {
            "type": "string",
            "pattern": "^[A-Z][a-zA-Z]*\\.[A-Z][a-zA-Z]*$"
          },
          "description": { "type": "string" }
        }
      }
    }
  }
}
