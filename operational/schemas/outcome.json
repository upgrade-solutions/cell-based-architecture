{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://dna.local/operational/outcome",
  "title": "Outcome",
  "description": "An Outcome describes what changes when a Capability successfully executes — state transitions, derived updates, and side effects like notifications or downstream capabilities.",
  "type": "object",
  "required": ["capability", "changes"],
  "properties": {
    "capability": {
      "type": "string",
      "description": "The Capability this outcome applies to, expressed as Noun.Verb.",
      "pattern": "^[A-Z][a-zA-Z]*\\.[A-Z][a-zA-Z]*$",
      "examples": ["Loan.Approve", "Order.Cancel", "Employee.Terminate"]
    },
    "description": {
      "type": "string",
      "description": "A human-readable explanation of what this outcome produces."
    },
    "changes": {
      "type": "array",
      "description": "Attribute changes applied to the noun after the capability executes.",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["attribute", "set"],
        "properties": {
          "attribute": { "type": "string" },
          "set": {
            "description": "The value to assign. Use 'now' for the current timestamp.",
            "examples": ["active", "now", true, null]
          }
        }
      }
    },
    "initiates": {
      "type": "array",
      "description": "Downstream capabilities to initiate after this capability completes.",
      "items": {
        "type": "string",
        "pattern": "^[A-Z][a-zA-Z]*\\.[A-Z][a-zA-Z]*$"
      }
    }
  }
}
