{
  "name": "@cell/event-bus-cell",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "echo 'event-bus-cell paused — skipped' && exit 0",
    "test": "echo 'event-bus-cell paused — skipped' && exit 0"
  },
  "dependencies": {
    "@dna/validator": "*"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.0",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.4.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "moduleNameMapper": {
      "^@dna/validator$": "<rootDir>/../../../packages/dna-validator/src/index.ts"
    }
  }
}
